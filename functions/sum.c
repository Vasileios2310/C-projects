#include <stdio.h>

int sum(int a , int b);

int main (void)
{
    int total;

    total = sum(5,6);
    printf("The sum is : %d\n" , total);

    return 0;
}

int sum(int a , int b)
{
    int s;
    return s = a + b;

}